import 'package:flutter/material.dart';

class DiagramTransactionsScreen extends StatelessWidget {
  const DiagramTransactionsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Diagram transactions screen')),
    );
  }
}
